# C-Uppercase-Lowercase-Alphabet-Checker
A simple C++ program that determines whether an entered alphabet character is uppercase or lowercase. Uses the &lt;cctype> library for character validation and classification.
